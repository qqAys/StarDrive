services:
  stardrive:
    build: .
    ports:
      - "127.0.0.1:8080:8080"
    volumes:
      - ./data:/opt/stardrive/app_data
    environment:
      - STARDRIVE_APP_SECRET="your-secret-here"
    restart: always